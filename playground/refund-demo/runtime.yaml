apiVersion: charter/v1
kind: RuntimePolicy

agent: refund-demo

per_run:
  max_cost_usd: 0.25
  max_llm_calls: 20
  max_tool_failures: 2
  max_queue_depth: 50
  tool_call_limits:
    - tool: desk__get_charge
      max_calls: 6
