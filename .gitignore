.venv/
venv/
__pycache__/
*.py[cod]
*.egg-info/
build/
dist/
.pytest_cache/
.ruff_cache/
.mypy_cache/
.DS_Store
.env
*.bak

# The demo network's state. Generated by running the leads pipeline, and the whole
# point is that it accumulates — a committed one would replay someone else's
# conversations as if they were yours.
demo/leads/network.db
